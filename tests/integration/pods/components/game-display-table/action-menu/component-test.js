import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('game-display-table/action-menu', 'Integration | Component | game display table/action menu', {
  integration: true
});

test('it renders', function(assert) {
  assert.equal(true, true);
});
