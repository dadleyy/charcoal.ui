import Ember from 'ember';

const { Component } = Ember;

function init() {
  this._super(...arguments);
}

const actions = {

  clear() {
  },

  remove() {
  }

};

export default Component.extend({ init, actions });
